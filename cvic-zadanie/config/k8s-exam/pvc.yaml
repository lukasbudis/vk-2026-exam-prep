apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ukf-pv-claim
  namespace: exam-budis
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
