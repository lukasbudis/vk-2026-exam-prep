apiVersion: v1
kind: Service
metadata:
  name: hello-app
  namespace: exam-budis-logapi
spec:
  selector:
    app: hello-app
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30081 
